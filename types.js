export default new Map(Object.entries({
  BibliographicReference: `metadata`,
  Language:               `metadata`,
  Lexeme:                 `data`,
  Person:                 `metadata`,
  Project:                `metadata`,
  Text:                   `data`,
  User:                   `metadata`,
}))
